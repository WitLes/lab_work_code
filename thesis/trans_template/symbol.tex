\documentclass{article}
\usepackage{amsmath, bm}
\begin{document}
	$\mathcal{N=(V,E)}$							% temporal network
	
	$\mathcal{N_S=(G,\rho)}$					% stochastic temporal network
	$\mathcal{N_S=(G,\bm{\rho})}$
	
	$\psi_{uv}(\tau)$							% inter-event time distribution
	
	$N$											% node number of network
	
	$\bm\rho_{uv}=\left\{ \rho_{uv}(\tau) \right\}_{(u,v)\in \mathcal{G}} $   %set of WTDs of a STN
	
	$\tau_{uv}$									% waiting time
	
	$\Phi_{uv}$									% survival function
	
	$D=\left\{ \mathcal{D}^i\right\}_{i=1,2,...,M}$
	
	$K_h(\tau)=\frac{1}{\sqrt{2\pi h^2}}exp(-\tau^2/2h^2)$        
	
	$\theta$
\end{document}S